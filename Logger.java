package week5CodingAssignmentSback;

public interface Logger {
 
	//signatures of methods we want any class that implements logger to have
	
	public void Log(String log); 
	public void Error(String error);
	
}
